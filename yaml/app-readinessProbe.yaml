apiVersion: apps/v1
kind: Deployment
metadata:
  name: example-readiness-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: example-readiness-app
  template:
    metadata:
      labels:
        app: example-readiness-app
    spec:
      containers:
        - name: example-readiness-container
          image: nginx:latest
          ports:
            - containerPort: 80
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
